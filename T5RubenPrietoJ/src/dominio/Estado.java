package dominio;

/**
 * Clase de enumeración con el que usamos los Estados
 */
public enum Estado {
	/**
	 * Clase de enumeración con el que usamos los Estados
	 */
	Pendiente, Resuelta, Eliminada;
}
